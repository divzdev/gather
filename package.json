{
  "name": "conference-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source speaker and session management for conferences",
  "license": "MIT",
  "workspaces": [
    "apps/web",
    "apps/embed"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
