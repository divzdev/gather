/** Placeholder until the Overview screen is ported from Overview.dc.html. */
export default function AdminPage() {
  return (
    <main style={{ padding: "20px 28px 80px" }}>
      <h1 style={{ font: "600 24px var(--font-plex-sans), sans-serif", margin: 0 }}>Overview</h1>
      <p style={{ color: "var(--i3)", marginTop: 6 }}>Being ported from the design set.</p>
    </main>
  );
}
