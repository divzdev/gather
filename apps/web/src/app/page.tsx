import { GatherLanding } from "@/components/design/GatherLanding";

export const metadata = {
  title: "Gather — the speaker side of your conference, off your plate",
};

export default function Page() {
  return <GatherLanding />;
}
